image: alpine/latest
secrets:
  - 3089eab4-03d1-41f1-85d7-80367dc7f105
environment:
  GH_REPO: srht-dotfiles.nix
  GH_USER: rogeruiz
  SH_REPO: .files.nix
tasks:
  - mirror-to-github: |
      cd ~/"${SH_REPO}"
      git config --global credential.helper store
      git push --mirror "https://github.com/${GH_USER}/${GH_REPO}"

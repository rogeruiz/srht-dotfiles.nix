image: alpine/latest
secrets:
  - 05adeb56-20d9-40f5-95ed-f6ec7213fa8b
environment:
  GH_REPO: srht-dotfiles.nix
  GH_USER: rogeruiz
  SH_REPO: .files.nix
tasks:
  - mirror-to-github: |
      cd ~/"${SH_REPO}"
      git config --global credential.helper store
      git push --mirror "https://github.com/${GH_USER}/${GH_REPO}"
